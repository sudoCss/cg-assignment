{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build & run opengl-app",
            "type": "shell",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "command": "make; ${workspaceRoot}/app"
        }
    ]
}
